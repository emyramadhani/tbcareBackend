version: '3.8'

services:
  api-gateway:
    build:
      context: ./api-gateway
      dockerfile: Dockerfile
    container_name: tbcare-api-gateway
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=development
      - JWT_SECRET=${JWT_SECRET:-rahasialokal123}
      - USER_SERVICE_URL=http://user-service:3001
      - EDUCATION_SERVICE_URL=http://education-service:3002
      - REMINDER_SERVICE_URL=http://reminder-medicine-service:3003
      - CHATBOT_SERVICE_URL=http://chatbot-service:3004
      - SKRINING_SERVICE_URL=http://skrining-service:3005
      - HEALTH_SERVICE_URL=http://health-service:3006
    depends_on:
      - user-service
      - reminder-medicine-service
      - education-service
      - skrining-service
      - chatbot-service
      - health-service
    networks:
      - tbcare-network
    volumes:
      - ./api-gateway/src:/app/src
    command: npm run dev

  user-service:
    build:
      context: ./user-service
      dockerfile: Dockerfile
    container_name: tbcare-user-service
    restart: unless-stopped
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - NODE_ENV=development
      - MONGO_URI=mongodb://host.docker.internal:27017/tbcare
      - JWT_SECRET=${JWT_SECRET:-rahasialokal123}
      - JWT_EXPIRES_IN=7d
    networks:
      - tbcare-network
    volumes:
      - ./user-service/src:/app/src
    command: npm run dev

  reminder-medicine-service:
    build:
      context: ./reminder-medicine-service
      dockerfile: Dockerfile
    container_name: tbcare-reminder-service
    restart: unless-stopped
    ports:
      - "3003:3003"
    environment:
      - PORT=3003
      - NODE_ENV=development
      - MONGO_URI=mongodb://host.docker.internal:27017/tbcare
      - JWT_SECRET=${JWT_SECRET:-rahasialokal123}
    networks:
      - tbcare-network
    volumes:
      - ./reminder-medicine-service/src:/app/src
    command: npm run dev

  education-service:
    build:
      context: ./education-service
      dockerfile: Dockerfile
    container_name: tbcare-education-service
    restart: unless-stopped
    ports:
      - "3002:3002"
    environment:
      - PORT=3002
      - NODE_ENV=development
      - MONGO_URI=mongodb://host.docker.internal:27017/tbcare
      - JWT_SECRET=${JWT_SECRET:-rahasialokal123}
    networks:
      - tbcare-network
    volumes:
      - ./education-service/src:/app/src
      - ./education-service/uploads:/app/uploads
    command: npm run dev

  skrining-service:
    build:
      context: ./skrining-service
      dockerfile: Dockerfile
    container_name: tbcare-skrining-service
    restart: unless-stopped
    ports:
      - "3005:3005"
    environment:
      - PORT=3005
      - NODE_ENV=development
      - MONGO_URI=mongodb://host.docker.internal:27017/tbcare
      - JWT_SECRET=${JWT_SECRET:-rahasialokal123}
    networks:
      - tbcare-network
    volumes:
      - ./skrining-service/src:/app/src
    command: npm run dev

  chatbot-service:
    build:
      context: ./chatbot-service
      dockerfile: Dockerfile
    container_name: tbcare-chatbot-service
    restart: unless-stopped
    ports:
      - "3004:3004"
    environment:
      - PORT=3004
      - NODE_ENV=development
      - MONGO_URI=mongodb://host.docker.internal:27017/tbcare
      - JWT_SECRET=${JWT_SECRET:-rahasialokal123}
    networks:
      - tbcare-network
    volumes:
      - ./chatbot-service/src:/app/src
    command: npm run dev

  health-service:
    build:
      context: ./health-service
      dockerfile: Dockerfile
    container_name: tbcare-health-service
    restart: unless-stopped
    ports:
      - "3006:3006"
    environment:
      - PORT=3006
      - NODE_ENV=development
      - MONGO_URI=mongodb://host.docker.internal:27017/tbcare
      - JWT_SECRET=${JWT_SECRET:-rahasialokal123}
    networks:
      - tbcare-network
    volumes:
      - ./health-service/src:/app/src
    command: npm run dev

networks:
  tbcare-network:
    driver: bridge

volumes:
  education_uploads:
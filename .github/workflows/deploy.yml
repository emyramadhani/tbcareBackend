name: Deploy to GCP VM

on:
  push:
    branches:
      - production

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Copy files via scp
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "."
          target: "/home/${{ secrets.USERNAME }}/tbcare-backend"
          rm: true

      - name: Execute Docker Compose via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            # Pindah ke folder proyek
            cd /home/${{ secrets.USERNAME }}/tbcare-backend
            
            # Matikan sistem yang lama
            docker-compose -f docker-compose.prod.yml down
            
            # Build dan hidupkan sistem yang baru dari kode teranyar
            docker-compose -f docker-compose.prod.yml up -d --build
            
            # Bersihkan cache docker agar disk server tidak penuh
            docker image prune -af